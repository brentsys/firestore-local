import { assert } from 'chai';
import { desc } from '../../lib/describer'

//setFixtures(fix)
//noDeleteDatabase()

//let cfg = Config(0)
desc("TEST SAMPLE", function() {
    it('should not delete database', function (done) {
        assert(true)
        done()
    });
});

